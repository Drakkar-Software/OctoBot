// Subset port of octobot_commons.constants — only the values the downstream
// packages reference.

export const MSECONDS_TO_SECONDS = 1000;
export const MINUTE_TO_SECONDS = 60;
export const HOUR_TO_SECONDS = 3600;
export const DAY_TO_SECONDS = 86400;

// Default config keys for portfolio sub-fields.
export const CONFIG_PORTFOLIO_FREE = "free";
export const CONFIG_PORTFOLIO_USED = "used";
export const CONFIG_PORTFOLIO_TOTAL = "total";
