class PackageManager:
    def __init__(self, config):
        self.config = config

    def parse_command(self, command):
        if command == "install":
            pass

        elif command == "":
