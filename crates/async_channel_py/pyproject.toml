[build-system]
requires = ["maturin>=1.7,<2.0"]
build-backend = "maturin"

[project]
name = "async-channel-rs"
version = "2.2.2"
requires-python = ">=3.12"

[tool.maturin]
features = ["pyo3/extension-module"]
bindings = "pyo3"
module-name = "async_channel_rs._core"
python-source = "python"
